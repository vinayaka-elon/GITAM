Given the list fruits = ["apple", "banana", "cherry", "date", "elderberry"], retrieve the first element.
Using the list numbers = [10, 20, 30, 40, 50, 60, 70], create a new list containing only the first three elements.
Combine the lists a = [1, 2, 3] and b = [4, 5, 6] into a single list.
Reverse the list colors = ["red", "blue", "green", "yellow"] without using loops or functions.
Replace the second element of the list names = ["John", "Jane", "Alice"] with "Bob".
Create a NumPy array arr with the elements [1, 2, 3, 4, 5].
Using the array arr = np.array([10, 20, 30, 40, 50]), retrieve the element at index 3.
Change the last element of the array arr = np.array([5, 10, 15, 20]) to 25.
Given the dictionary person = {"name": "Alice", "age": 30, "city": "New York"}, access the value associated with the key "age".
Add a new key-value pair "occupation": "Engineer" to the dictionary person = {"name": "Alice", "age": 30}.
